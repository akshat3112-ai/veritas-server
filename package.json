{
  "name": "veritas-server",
  "version": "1.0.0",
  "description": "API proxy for Veritas fact-checker — hides API keys from the app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  }
}
